(function(window, angular) {
    'use strict';

    // Your starting point. Enjoy the ride!

    //6 创建主模块
    var app = angular.module('todoApp', [
        'todoApp.controller'
        /* 有多个模块 继续添加 */
    ]);


})(window, angular);